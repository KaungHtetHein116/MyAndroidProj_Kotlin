<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mymultiplescreen">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyMultipleScreen">
        <activity android:name=".RecyclerListActivity"></activity>
        <activity android:name=".ListViewActivity" />
        <activity android:name=".PlayGroundActivity" />
        <activity
            android:name=".SecondIntent"
            android:parentActivityName=".IntentActivity" />
        <activity
            android:name=".IntentActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".GameActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".LifeCycleActivity" />
        <activity android:name=".MaterialDesignActivity" />
        <activity android:name=".ImageGuessActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>